---
"@epodivilov/vikunja-mcp": minor
---

Add `vikunja_get_board` (read a project's kanban board as ordered columns of lean tasks, both manual and filter modes) and `vikunja_move_task` (move a task into a named column on a manual-bucket board).
